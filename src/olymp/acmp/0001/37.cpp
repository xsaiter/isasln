#include <bits/stdc++.h>

using namespace std;

int main() {
  ios::sync_with_stdio(0);
  cin.tie(0);

  string s = "abcab";
  int n = s.size();
  set<char> se;
  for (int i = 0; i < n; ++i) {
  }

  /*int n;
  double q;
  cin >> n >> q;*/
  return 0;
}