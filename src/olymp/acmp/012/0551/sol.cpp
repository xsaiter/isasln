#include <bits/stdc++.h>

using namespace std;

bool solve(int R, int r, int h, int b) {
  int d = 
  return false;
}

int main() {
  int R, r, h, b;
  cin >> R >> r >> h >> b;
  cout << (solve(R, r, h, b) ? "YES" : "NO") << endl;
  return 0;
}