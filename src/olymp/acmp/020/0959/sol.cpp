#include <bits/stdc++.h>

using namespace std;

struct P {
  int x, y;
};

int solve(int n, int a, int b, int c, vector<P> &d) {
  int ans = 0;
  return ans;
}

int main() {
  int n;
  cin >> n;
  int a, b, c;
  cin >> a >> b >> c;
  vector<P> d(n);
  for (int i = 0; i < n; ++i) {
    cin >> d[i].x >> d[i].y;
  }
  return 0;
}