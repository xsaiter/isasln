#include <bits/stdc++.h>

using namespace std;

int solve(int n, int m, int y, int x) {
  int r = 0;

  return r;
}

int main() {
  int n, m, y, x;
  cin >> n >> m >> y >> x;
  cout << solve(n, m, y, x) << endl;
  return 0;
}