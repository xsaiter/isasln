#include <bits/stdc++.h>

using namespace std;

struct P {
  int x, y;
};

struct Seg {
  P a, b;
};

int solve(Seg &s) {
  int ans = 0;

  return ans;
}

int main() {
  Seg s;
  cin >> s.a.x >> s.a.y;
  cin >> s.b.x >> s.b.y;
  cout << solve(s) << endl;
  return 0;
}