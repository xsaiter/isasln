#include<bits/stdc++.h>

using namespace std;

int solve(const string &s, int n) {  
  set<string> ss;
  for (int i = 0; i < n; ++i) {   
    for (int j =0; j < n; ++j) {
      
    }
  }
  return ss.size();
}

int main() {
  string s;
  cin >> s;
  int n = (int)s.size();
  cout << solve(s, n) << endl;
  return 0;
}