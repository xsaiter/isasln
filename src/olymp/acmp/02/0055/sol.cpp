#include <bits/stdc++.h>

using namespace std;

struct P {
  int x, y;
};

bool solve(P &c1, P &c2, int r, int s) {
  return false;
}

int main() {
  P c1, c2;
  cin >> c1.x >> x1.y;
  cin >> c2.x >> x2.y;
  int r;
  cin >> r;
  int s;
  cin >> s;
  cout << (solve(c1, c2, r, s) ? "YES" : "NO");
  return 0;
}