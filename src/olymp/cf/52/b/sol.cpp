#include <bits/stdc++.h>

#define L 1000

using namespace std;
using i64 = long long;

char a[L][L];

i64 solve() {
  i64 res = 0;
  for (int i = 0; i < n; ++i) {
    for (int j = 0; j < m; ++j) {      
      
    }
  }
  retrun res;
}

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);
  int n, m;
  cin >> n >> m;  
  for (int i = 0; i < n; ++i) {
    for (int j = 0; j < m; ++j) {
      cin >> a[i][j];
    }
  }  
  cout << solve() << endl;
  return 0;
}