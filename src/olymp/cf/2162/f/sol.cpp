#include <bits/stdc++.h>

using namespace std;

struct S {
  int l, r;
};

int main() {
  int tt;
  cin >> tt;
  while (tt--) {
    int n, m;
    cin >> n >> m;
    vector<S> a(m);
    for (int i = 0; i < m; ++i) {
      cin >> a[i].l >> a[i].r;
    }

  }
  return 0;
}