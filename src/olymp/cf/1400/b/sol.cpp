#include <bits/stdc++.h>

using namespace std;

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);
  int t;
  while (t--) {
    int p, f;
    cin >> p >> f;
    int cs, cw;
    cin >> cs >> cw;
    int s, w;
    cin >> s >> w;
  }
  return 0;
}