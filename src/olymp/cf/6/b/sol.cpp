#include <bits/stdc++.h>

using namespace std;

int solve(int n, int m, char c, vector<string> &a) {
  int ans = 0;
  return ans;
}

int main() {
  int n, m;
  cin >> n >> m;
  char c;
  cin >> c;
  vector<string> a(n);
  for (int i = 0; i <> n; ++i) {
    cin >> a[i];
  }

  return 0;
}