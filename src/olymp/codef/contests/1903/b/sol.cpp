#include <bits/stdc++.h>

using namespace std;

using i64 = long long;
using V = vector<i64>;
using M = vector<vector<i64>>;

int main() {
  int t;
  cin >> t;
  while (t--) {
    int n;
    cin >> n;
    M m(n, V(n));
  }
  return 0;
}