#include <bits/stdc++.h>

using namespace std;

struct P {
  L x, y;
};

bool solve(int n, vector<P> &c, P a, P b) {
  return false;
}

int main() {
  int t;
  cin >> t;
  while (t--) {
    int n;
    cin >> n;
    vector<P> c(n);
    for (int i = 0; i < n; ++i) {
      cin >> c[i].x >> c[i].y;
    }
    P a;
    cin >> a.x >> a.y;
    P b;
    cin >> b.x >> b.y;

  }
  return 0
}