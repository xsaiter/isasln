#include <bits/stdc++.h>

using namespace std;

string solve(string &s) {
  string res;
  int n = (int) s.size();

  return res;
}

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);
  string s;
  cin >> s;
  cout << solve(s) << endl;
  return 0;
}