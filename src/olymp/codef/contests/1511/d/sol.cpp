#include <bits/stdc++.h>

using namespace std;

string solve(int n, int k) {  	
  string s(n, 'a');
  for (int i = 0; i < n; ++i) {
  }
  return s;
}

int main() {
  int n, k;
  cin >> n >> k;
  cout << solve(n, k) << endl;
  return 0;
}