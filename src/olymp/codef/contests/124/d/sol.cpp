#include <bits/stdc++.h>

using namespace std;

struct P {
  int x, y;
};

int solve(int a, int b, P &p, P &q) {
  int res = 0;
  return res;
}

int main() {
  int a, b;
  cin >> a >> b;
  P p, q;
  cin >> p.x >> p.y >> q.x >> q.y;  
  cout << solve(a, b, p, q) << endl;
  return 0;
}