#include <bits/stdc++.h>

using namespace std;

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);
  int n, p, w, d;
  cin >> n >> p >> w >> d;
  vector<int> v;
  string s;  
  // x * w + y * d = p
  // x + y + z = n;

  return 0;
}