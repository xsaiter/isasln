#include <bits/stdc++.h>

using namespace std;

int solve(int n, int b, int c) {
  // a[i] = b * (i - 1) + c;
  int res = -1;
  return res;
}

int main() {
  int t;
  cin >> t;
  while (t--) {
    int n, b, c;
    cin >> n >> b >> c;
    cout << solve(n, b, c) << '\n';
  }
  cout << endl;
  return 0;
}