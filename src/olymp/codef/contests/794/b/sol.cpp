#include <bits/stdc++.h>

using namespace std;

dobule area_trap(double a, double b, double h) {
  return 0.5 * h * (a + b);
}



int main() {
  int n, h;
  cin >> n >> h;  
  vector<double> x(n);
  double ss = 0.5 * h * 1;
  double s = ss / n;

  return 0;
}