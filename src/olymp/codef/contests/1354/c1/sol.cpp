#include <bits/stdc++.h>

using namespace std;

using R = double;

R solve(int n) {
  R res = 0;
  return res;
}

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);
  int t;
  cin >> t;
  while (t--) {
    int n;
    cin >> n;
    cout << solve(n) << "\n";
  }
  cout << endl;
  return 0;
}