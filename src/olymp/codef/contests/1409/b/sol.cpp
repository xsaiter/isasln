#include <bits/stdc++.h>

using namespace std;
using LL = long long;

LL solve(int a, int b, int x, int y, int n) {
  LL res = 0;
  return res;
}

int main() {
  int t;
  cin >> t;
  while (t--) {
    int a, b, x, y, n;
    cin >> a >> b >> x >> y >> n;
    cout << solve(a, b, x, y, n) << "\n";
  }
  cout << endl;
  return 0;
}