#include <bits/stdc++.h>

using namespace std;

struct Res {
  int m, k;
};

Res solve(vecotr<int> &a, int n) {
  Res res;

  return res;
}

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);
  int t;
  cin >> t;
  while (t--) {
    cin >> t;
    int n;
    cin >> n;
    vector<int> a(n);
    for (int i = 0; i < n; ++i) {
      cin >> a[i];
    }
  }
  return 0;
}