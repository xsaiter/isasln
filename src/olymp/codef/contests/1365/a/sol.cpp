#include <bits/stdc++.h>

using namespace std;

constexpr int N = 50;

int a[N][N];

int solve(int n, int m) {
  for
  return 0;
}

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);
  int t;
  cin >> t;
  while (t--) { 
    int n, m;
    cin >> n >> m;
    for (int i = 0; i < n; ++i) {
      for (int j = 0; j < m; ++j) {
        cin >> a[i][j];
      }
    }
  }  
  return 0;
}
