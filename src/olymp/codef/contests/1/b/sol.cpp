#include <bits/stdc++.h>

using namespace std;

vector<string> solve(int n, vector<string> &a) {
  vector<string> b(n);
  return b;
}

int main() {
  int n;
  cin >> n;
  vector<string> a(n);
  for (int i = 0; i < n; ++i) {
    cin >> a[i];
  }
  vector<string> b = solve(n, a);
  return 0;
}