#include <bits/stdc++.h>

using namespace std;

int cross_prod(int ax, int ay, int bx, int by, int cx, int cy) {
  return 0;
}

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);
  int n;
  cin >> n;
  vector<int> x(n), y(n);
  for (int i = 0; i < n; ++i) {
    cin >> x[i] >> y[i];
  }
  vector<int> hx, hy;
  int res = 0;
  cout << res << endl;
  return 0;
}
