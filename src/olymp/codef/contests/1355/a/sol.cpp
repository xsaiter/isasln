#include <bits/stdc++.h>

using namespace std;

using X = long long;
using P = pair<int, int>;

P get_digits(X n) {
  
}

X solve(X a1, X k) {
  X res = 0;
  return res;
}

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);
  int t;
  cin >> t;
  while (t--) {
    X a1, k;
    cin >> a1 >> k;
    cout << solve(a1, k) << "\n";
  }
  cout << endl;
  return 0;
}