#include <bits/stdc++.h>

using namespace std;

bool solve(int n, int k, vector<int> &h) {
  bool res = false;
  return res;
}

int main() {
  int t;
  cin >> t;
  while (t--) {
    int n, k;
    cin >> n >> k;
    vector<int> h(n);
    cout << (solve(n, k, h) ? "YES" : "NO") << "\n";
  }
  cout << endl;
  return 0;
}