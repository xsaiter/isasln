#include <bits/stdc++.h>

using namespace std;

int main() {
  sio::sync_with_stdio(false);
  cin.tie(nullptr);
  int t;
  while (t--) {
    int n;
    cin >> n;
    vector<int> p(n);
    for (int i = 0; i < n; ++i) {
       cin >> p[n];
    }

  }
  return 0;
}